# Handoff — Estado para chat nuevo (18 jul 2026)

> Para un chat nuevo: "Lee HANDOFF_2026-07-18.md en vero-quant-lab para el contexto." Supersede al HANDOFF_2026-07-15. Resume la sesión del 17-18 jul sobre VQL.

## Qué es esto

Vero Quant Lab (VQL) = cockpit de trading sobre Binance (visor, nunca ejecuta). El sistema de trading = bots `vero-*` en el VPS Hetzner. Dos agentes, no cruzarlos: **VQL** (`~/dev/vero-quant-lab`, la app) y **Trading** (`~/Trading`, los `vero-*`, con ssh al VPS).

## Qué se hizo esta sesión

Todo commiteado en `main`. Nueve commits nuevos desde `0192717 (v1.5.0)`:

- **#39 Paridad del Checklist A+ — cerrado.** El panel del Chart calculaba su propia lógica y divergía del detector en 7 dimensiones (timeframe, vela en formación, gate de liquidez, mom2, VolR, VWAP, resistencia). Ahora consume `/api/aplus-live`, la MISMA fuente que el cockpit y el bot, byte-fiel a `calc_indicators.js`. Evalúa ETH 1m siempre. Se eliminó `lib/chart/checklist.ts`.
  - Reconciliado contra el `.pine` y el detector (repo Trading). `live.ts` = byte-fiel al detector. Se corrigió el paso 4 de tendencia: exige `EMA9−EMA21 ≥ 0.25×ATR` (antes `e9>e21`), igual que `detector_servicio.sh` (TREND_ATR=0.25).
  - Auditoría completa en `docs/AUDIT_39_paridad_checklist.md`.
- **Zonas S/R sobre el Chart A+ — cerrado.** Dibujadas desde `/api/zonas` (zonas.env), reemplazan el dibujo en TradingView.
- **#26 badge Order Flow móvil — cerrado.** Media query `<=640px`.
- **Reconciliación PnL realizado — cerrado.** Historial usa `lib/pnl/realized-pnl` (byte-idéntico al bot).
- **Fase C — cerrada y verificada de punta a punta.** `status_server.cjs` (repo Trading) corre como `vero-statusapi` en el VPS (127.0.0.1:8787, token). VQL cableado (`VPS_API_URL` en el `.env.local` del VPS). Contrato de los 3 endpoints (`/api/status`, `/api/telegram`, `/api/feed`) verificado byte-compatible. Operations, tarjeta VPS y "Señal A+ en vivo" muestran datos reales.
- **#29 botón Compartir — cerrado.** Snapshot del estado A+ (9 filtros + veredicto + precio + zonas) imprimible a PDF, en el Chart. Sin dependencias (`window.print` aislado por CSS).
- **#28 panel de Noticias — cerrado.** Nueva sección `/news`: RSS público (CoinDesk/Cointelegraph/Decrypt), sin API key, cache 10 min. Informativo.

## ⚠️ Acción inmediata pendiente

Los últimos commits (incluidas las features **#28 y #29**) están en `main` local pero **falta push + redeploy** para que lleguen a vql.economics.cl:

```
# Mac:
git push origin main
# VPS (root@ubuntu):
cd /root/vero-quant-lab && git pull origin main && npm run build && systemctl restart vero-vql && git log --oneline -1
```

El `git log` final debe mostrar `180b298` (o superior). Reiniciar **solo `vero-vql`**, nunca los `vero-*` de trading.

## Estado del sistema

- Dominio: https://vql.economics.cl (Caddy + basic_auth, user vero). VQL corre como `vero-vql` (:3000) en el VPS. `vero-statusapi` (:8787, localhost) alimenta Operations/feed.
- Repo: `github.com/vero-pix/vero-quant-lab`. VPS = checkout git en `/root/vero-quant-lab`, arranca con `npm start` (next start).
- El A+ del panel, el cockpit y el bot dan el MISMO número (misma fuente `/api/aplus-live`).

## Pendientes

**Trading (vía Agente Trading / Claude Code en `~/Trading`):**
- **Limpieza del bot Telegram** (prompt listo: `PROMPT_telegram_cleanup_trading.md`). El comando `posicion`/`pos` consulta Capital.com (retirado) — quitar. Más serio: el bot arrastra toda la ruta de ejecución por Capital (`capital_client`, `capital_order`, spread $1.75) — confirmar si `cl.vero.telegrambot` sigue corriendo esa vía y limpiarla o apagarla (el sistema real ejecuta por `binance_autoexec.cjs` + `/auto`).
- **Cargar `senal_aplus_completa.pine` en TradingView** para compilar (el agente ya lo alineó al detector). Paso manual.
- Capa 2: redimir ETH staking→Spot antes de vender (#32, no urgente).

**VQL:** sin pendientes de código. Backlog menor: snapshot de equity de apertura, homologar LabService, piso $5 límite diario.

**A+ (el norte):** mejorar los resultados está **gated en DATA**, no en trabajo. Hoy ~15 días; se necesitan 30-60 para el barrido + walk-forward. Recalibrar antes es curve-fitting. **Gatillo:** cuando el historial acumule ≥30 días, correr la sesión de calibración (R-006, solo barrido automatizado + walk-forward, nunca a mano).

## Reglas de oro

1. VQL es visor — nunca ejecuta ni escribe en el sistema de trading.
2. Los `vero-*` son sagrados — nunca reiniciar en mercado / con posición abierta. `vero-vql` y `vero-statusapi` son aparte (esos sí se reinician).
3. No recalibrar a mano — solo barrido + walk-forward con 30-60 días.
4. Las compras manuales en Binance quedan desnudas (sin stop); el Guardian las marca.

## Infra rápida

- VPS: `root@159.69.202.179`, ssh key `~/.ssh/hetzner_trading` (en el Mac; el sandbox NO la tiene).
- `zonas.env`: `~/Trading/tradingview-mcp/scripts/zonas.env`. Control mantenedor: `~/Trading/control/autoexec_control.json`.
- Telegram: token/chat en `~/Trading/.env.telegram` (nunca commitear).
- Prompts generados esta sesión (en outputs): Pine, Fase C, Telegram cleanup.
